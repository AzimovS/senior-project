#start of readme
Create a new environment with conda (or any other). The python version is 3.8
	conda create -n lfc_verification python=3.8

Install all libraries from requirements.txt with

pip install -r requirements.txt

To start the app

python main.py

#end of readme